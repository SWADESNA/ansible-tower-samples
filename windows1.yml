---
- name: Test windows
  hosts: all
  tasks:
    - name: ping
      win_ping:
    - name: Ensure user bob is present
      win_user:
        name: Bob
        password: Bobpassword
        state: present
        groups:
          - Users
